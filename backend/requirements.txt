# Deployment Doctor — Backend Dependencies
# Only direct application dependencies are listed.
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.110.1
uvicorn[standard]==0.25.0
python-multipart==0.0.20
starlette==0.37.2

# Configuration
python-dotenv==1.2.2

# Data validation
pydantic==2.11.5

# Database — PostgreSQL via SQLAlchemy async
SQLAlchemy==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10

# HTTP client — OpenRouter AI summary (standard REST, no proprietary SDK)
httpx==0.28.1

# Testing
pytest==8.3.5
pytest-asyncio==0.24.0
pytest-cov==5.0.0
